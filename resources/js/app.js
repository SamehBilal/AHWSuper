/**
 * Echo exposes an expressive API for subscribing to channels and listening
 * for events that are broadcast by Laravel. Echo and event broadcasting
 * allow your team to quickly build robust real-time web applications.
 */

import './echo';
import './calendar';
/*
document.addEventListener('DOMContentLoaded', theme);

// Initialize calendar when Livewire updates the DOM
document.addEventListener('livewire:navigated', theme);
document.addEventListener('livewire:load', theme);

// Additional Livewire 3 specific events
document.addEventListener('livewire:initialized', theme);
document.addEventListener('livewire:navigating', () => {
    // Add a small delay to ensure the calendar is initialized after navigation
    theme
});

function theme() {
    const savedTheme = localStorage.getItem('mary-theme')?.replace(/"/g, '') || 'light';
    document.documentElement.setAttribute('data-theme', savedTheme);
    document.body.setAttribute('data-theme', savedTheme);
}; */

